# ============================================
# Amazon Q to API - 环境变量配置示例
# ============================================

# ------------------------------------------
# 通用配置
# ------------------------------------------

# 是否启用无头模式（不显示浏览器窗口）
# 默认: false（显示浏览器窗口）
HEADLESS=false

# 账户信息输出文件路径
# 默认: output/accounts.ndjson
OUTPUT_FILE=output/accounts.ndjson

# 日志级别: debug | info | warn | error
# 默认: info
LOG_LEVEL=info

# 浏览器引擎: playwright (Chrome) | camoufox (Firefox 反检测)
# 默认: camoufox
BROWSER_ENGINE=camoufox

# ------------------------------------------
# 账户模式
# ------------------------------------------

# 账户模式: manual (手动登录) | temp-email (临时邮箱注册)
# 默认: manual
ACCOUNT_MODE=temp-email

# ------------------------------------------
# AWS 凭据 (手动登录模式)
# ------------------------------------------

# AWS 账户邮箱 (手动模式必需)
AWS_EMAIL=

# AWS 账户密码 (必需)
AWS_PASSWORD=

# AWS MFA 密钥 (可选，用于自动处理 TOTP 验证)
AWS_MFA_SECRET=

# ------------------------------------------
# 临时邮箱注册模式
# ------------------------------------------

# 临时账户密码 (可使用 AWS_PASSWORD 代替)
AWS_TEMP_PASSWORD=

# 注册用全名 (可选)
AWS_FULL_NAME=

# ------------------------------------------
# GPTMail 配置 (临时邮箱模式必需)
# ------------------------------------------

# GPTMail API 密钥 (可使用 GPTMAIL_KEY 作为别名)
GPTMAIL_API_KEY=gpt-test

# GPTMail 服务地址
# 默认: https://mail.chatgpt.org.uk
GPTMAIL_BASE_URL=https://mail.chatgpt.org.uk

# 邮箱前缀 (可选，用于生成邮箱地址)
GPTMAIL_EMAIL_PREFIX=

# 邮箱域名 (可选，用于生成邮箱地址)
GPTMAIL_EMAIL_DOMAIN=

# 邮件轮询间隔 (毫秒，可选)
GPTMAIL_POLL_INTERVAL_MS=

# 邮件等待超时 (毫秒，可选)
GPTMAIL_TIMEOUT_MS=

# ------------------------------------------
# 浏览器 Profile 配置 (指纹隔离)
# ------------------------------------------

# 是否启用浏览器指纹隔离
# 默认: true
PROFILE_ENABLED=true

# Profile 存储文件路径
# 默认: output/profiles.json
PROFILE_STORE_PATH=output/profiles.json

# 是否启用反检测脚本
# 默认: true
PROFILE_STEALTH=true

# ------------------------------------------
# 代理配置
# ------------------------------------------

# 代理列表 (逗号或换行分隔，支持多代理轮询)
# 格式: http://user:pass@host:port 或 socks5://host:port
PROXY_LIST=

# HTTP 代理 (单个代理，也可使用小写 http_proxy)
HTTP_PROXY=
